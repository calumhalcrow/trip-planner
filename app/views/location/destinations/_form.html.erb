<%= form_for(@location_destination) do |f| %>
  <% if @location_destination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_destination.errors.count, "error") %> prohibited this location_destination from being saved:</h2>

      <ul>
      <% @location_destination.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
