<h1>Listing location_destinations</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @location_destinations.each do |location_destination| %>
  <tr>
    <td><%= link_to 'Show', location_destination %></td>
    <td><%= link_to 'Edit', edit_location_destination_path(location_destination) %></td>
    <td><%= link_to 'Destroy', location_destination, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Destination', new_location_destination_path %>
