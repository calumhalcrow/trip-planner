<%= form_for(@location_home) do |f| %>
  <% if @location_home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_home.errors.count, "error") %> prohibited this location_home from being saved:</h2>

      <ul>
      <% @location_home.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
